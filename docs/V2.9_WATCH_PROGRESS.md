# ASUMI v2.9.0 — WATCH ENGINE (TIẾN ĐỘ & BÀN GIAO HIỆN TẠI)

> **Thời điểm ghi nhận:** 25/09/2026 16:25 GMT+7  
> **Git Branch:** [`feat/asumi-watch-v2.9-wip`](https://github.com/HoenMike/DiscordMikeBot/tree/feat/asumi-watch-v2.9-wip)  
> **Commit ID:** `c3e0948` (và commit lưu tài liệu này)  
> **Mục tiêu phiên bản:** `2.8.0` $\rightarrow$ `2.9.0` (Codename: *"Asumi Watch — Persistent Web Monitoring & Smart Alerts"*)  

---

## 1. TỔNG QUAN TÌNH TRẠNG HIỆN TẠI (EXECUTIVE SUMMARY)

Toàn bộ kiến trúc và tính năng cốt lõi của **Asumi Watch Engine** đã được hoàn thành 100% mã nguồn, bao gồm:
1. **Kiến trúc module riêng biệt** tại `features/watch/` không phụ thuộc thêm thư viện ngoài (tái sử dụng `core.db.db_client`, `core.ai.bounded_ai_generate`, `discord.ext.tasks.loop`).
2. **Cơ chế quản lý hạn ngạch & Search Budget**: Hạn ngạch cứng 900 request/tháng, điều tiết mềm theo ngày, bộ nhớ đệm SearchCache TTL 6 giờ dùng chung giữa các Watch có cùng truy vấn tìm kiếm.
3. **Lưu trữ bền vững**: 4 bảng SQLite/Turso Cloud tương thích (`watch_definitions`, `watch_results`, `watch_runs`, `watch_search_usage`) kèm các index tối ưu truy vấn chu kỳ.
4. **Đường ống Delta-first & Silent Baseline**: 
   - Lần đầu chạy chỉ thiết lập ngữ cảnh mốc nền, không spam tin nhắn cũ.
   - Không có kết quả mới $\rightarrow$ Không gọi Gemini $\rightarrow$ Không gửi tin nhắn Discord.
5. **Độ tin cậy Discovered != Evaluated**: Kết quả tìm kiếm mới được bảo lưu bền vững. Nếu Gemini gặp lỗi mạng/timeout, kết quả vẫn giữ nguyên trạng thái để lần chạy sau đánh giá lại mà **không tốn thêm request tìm kiếm Brave**.
6. **Phòng vệ Prompt-Injection**: System instruction trong AI Evaluator xác định rõ mọi kết quả tìm kiếm ngoại lai là dữ liệu không tin cậy, bỏ qua mọi câu lệnh can thiệp nằm trong snippet.
7. **Tương tác Discord hoàn chỉnh**: Bộ lệnh Slash Command `/watch` (`create` kèm Modal UI, `list`, `view`, `pause`, `resume`, `delete`, `run-now`, `budget`). Khởi động an toàn ngay cả khi chưa có `BRAVE_SEARCH_API_KEY` (không kích hoạt Slash Command Safety Lock).
8. **Web Dashboard Admin Console**: Bổ sung tab Watch chuyên sâu với 8 chỉ số tổng quan, danh sách quản lý, modal chi tiết compact state và nhật ký chạy `watch_runs`.

---

## 2. DANH SÁCH TỆP ĐÃ TẠO MỚI & CHỈNH SỬA

### Tệp tạo mới:
- [`features/watch/__init__.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/__init__.py): Package init.
- [`features/watch/models.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/models.py): Dataclasses & Enums (`WatchDefinition`, `WatchResult`, `WatchRun`, `SearchUsage`, `EvaluationResult`, etc.).
- [`features/watch/constants.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/constants.py): Hằng số chu kỳ (`6h`, `24h`, `72h`, `168h`), giới hạn tối thiểu 4h, màu embed `0x3B82F6`, câu thông báo thân thiện.
- [`features/watch/search.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/search.py): `canonicalize_url`, `compute_fingerprint`, in-memory `SearchCache`, `BraveSearchProvider` (aiohttp client, transient retry, error mapping), `calculate_daily_allowance`.
- [`features/watch/manager.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/manager.py): Singleton `watch_manager`, khởi tạo 4 bảng DB, CRUD Watch, quản lý limits (5 per user, 30 global), budget gate, pruning kết quả cũ, thống kê dashboard.
- [`features/watch/evaluator.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/evaluator.py): Gọi `bounded_ai_generate()` với `GEMINI_DATA_MODEL`, prompt-injection defense, JSON parser xử lý cả Markdown code fences.
- [`features/watch/notifier.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/notifier.py): Dựng embed Discord trang nhã, trích xuất 1–3 nguồn uy tín, `AllowedMentions.none()`, nhãn thông báo điều kiện hoàn tất.
- [`features/watch/scheduler.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/scheduler.py): Heartbeat loop 15 phút, `asyncio.Semaphore(2)`, offline catch-up chống bão request, quy trình first-run baseline, delta pipeline, quản lý vòng đời `cog_unload`.
- [`features/watch/cog.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/features/watch/cog.py): App command group `/watch`, modal UI, cooldown 60s cho run-now, kiểm tra quyền sở hữu.
- [`tests/test_watch.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/tests/test_watch.py): Bộ kiểm thử tự động toàn diện bao phủ toàn bộ các yêu cầu từ Section 63 đến Section 73.

### Tệp chỉnh sửa:
- [`bot_instance.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/bot_instance.py): Thêm `features.watch.cog` vào `FEATURE_EXTENSIONS`, thêm `"watch"` vào `EXPECTED_CORE_SLASH_COMMANDS`, cập nhật HelpView & lệnh trợ giúp.
- [`config.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/config.py): Thêm 10 cấu hình Watch (`BRAVE_SEARCH_API_KEY`, `WATCH_ENABLED`, `WATCH_MONTHLY_SEARCH_BUDGET=900`, etc.).
- [`core/activity_logger.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/core/activity_logger.py): Thêm tracking hành động `watch`.
- [`core/version.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/core/version.py): Cập nhật `CURRENT_VERSION = "2.9.0"`, thêm mục Changelog v2.9.0 ở đầu danh sách, bảo toàn nguyên vẹn v2.8.0.
- [`web/app.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/web/app.py): Thêm 8 endpoints REST API quản trị `/api/watch/*`.
- [`web/templates/dashboard.html`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/web/templates/dashboard.html): Thêm tab Watch, 8 thẻ chỉ số, bảng quản lý, modal chi tiết và JavaScript kết nối dữ liệu thời gian thực.
- [`.env.example`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/.env.example): Hướng dẫn cấu hình biến môi trường cho Watch Engine.
- [`README.md`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/README.md): Cập nhật tài liệu tính năng, danh mục lệnh `/watch` và cấu trúc mã nguồn.
- [`tests/test_release28.py`](file:///c:/Users/nguyenhoang.mai/Documents/Codes/DiscordMikeBot/tests/test_release28.py): Cập nhật assertion phiên bản hỗ trợ 2.9.0.

---

## 3. CƠ SỞ DỮ LIỆU & SCHEMA (TURSO / LOCAL SQLITE)

Hệ thống tự động chạy `CREATE TABLE IF NOT EXISTS` và `CREATE INDEX IF NOT EXISTS`:

```sql
-- 1. Định nghĩa mục tiêu theo dõi
CREATE TABLE IF NOT EXISTS watch_definitions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    owner_user_id INTEGER NOT NULL,
    guild_id INTEGER,
    channel_id INTEGER NOT NULL,
    title TEXT NOT NULL,
    search_query TEXT NOT NULL,
    condition_prompt TEXT,
    status TEXT NOT NULL DEFAULT 'active',
    cadence_hours INTEGER NOT NULL DEFAULT 24,
    next_run_at TEXT,
    last_checked_at TEXT,
    last_notified_at TEXT,
    state_json TEXT DEFAULT '{}',
    notify_only_on_change INTEGER NOT NULL DEFAULT 1,
    stop_after_trigger INTEGER NOT NULL DEFAULT 0,
    failure_count INTEGER NOT NULL DEFAULT 0,
    last_error TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_watch_due ON watch_definitions(status, next_run_at);
CREATE INDEX IF NOT EXISTS idx_watch_owner ON watch_definitions(owner_user_id);

-- 2. Bộ nhớ lưu kết quả tìm kiếm bền vững
CREATE TABLE IF NOT EXISTS watch_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    watch_id INTEGER NOT NULL,
    fingerprint TEXT NOT NULL,
    url TEXT NOT NULL,
    canonical_url TEXT NOT NULL,
    title TEXT NOT NULL,
    snippet TEXT,
    source_domain TEXT,
    published_at TEXT,
    discovered_at TEXT NOT NULL,
    evaluation_status TEXT NOT NULL DEFAULT 'discovered',
    event_fingerprint TEXT,
    metadata_json TEXT DEFAULT '{}',
    UNIQUE(watch_id, fingerprint)
);
CREATE INDEX IF NOT EXISTS idx_watch_res_eval ON watch_results(watch_id, evaluation_status);

-- 3. Nhật ký thực thi chi tiết phục vụ quan sát (Observability)
CREATE TABLE IF NOT EXISTS watch_runs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    watch_id INTEGER NOT NULL,
    started_at TEXT NOT NULL,
    finished_at TEXT NOT NULL,
    status TEXT NOT NULL,
    search_performed INTEGER NOT NULL DEFAULT 0,
    cache_hit INTEGER NOT NULL DEFAULT 0,
    search_result_count INTEGER NOT NULL DEFAULT 0,
    new_result_count INTEGER NOT NULL DEFAULT 0,
    ai_called INTEGER NOT NULL DEFAULT 0,
    meaningful_change INTEGER NOT NULL DEFAULT 0,
    notification_sent INTEGER NOT NULL DEFAULT 0,
    duration_ms REAL NOT NULL DEFAULT 0.0,
    error_text TEXT
);
CREATE INDEX IF NOT EXISTS idx_watch_runs_wid ON watch_runs(watch_id, started_at);

-- 4. Hạn ngạch sử dụng tìm kiếm theo tháng
CREATE TABLE IF NOT EXISTS watch_search_usage (
    month_key TEXT PRIMARY KEY,
    search_requests INTEGER NOT NULL DEFAULT 0,
    cache_hits INTEGER NOT NULL DEFAULT 0,
    ai_evaluations INTEGER NOT NULL DEFAULT 0,
    notifications_sent INTEGER NOT NULL DEFAULT 0,
    updated_at TEXT NOT NULL
);
```

---

## 4. TRẠNG THÁI KIỂM THỬ (TEST STATUS)

- **`python -m compileall .`**: ✅ **PASS 100% (0 lỗi cú pháp)**
- **`git diff --check`**: ✅ **PASS 100% (0 lỗi định dạng)**
- **Bộ test v2.8 hồi quy (`tests/test_embed_audit.py`, `tests/test_release28.py`)**: ✅ **PASS 26/26 tests (100%)**
- **Live Brave Requests dùng trong quá trình code**: **`0 request`** (100% mock an toàn, không tốn quota thật).
- **Bộ test Watch (`tests/test_watch.py`)**:
  - Đã pass **21 / 28 tests** (Bao gồm toàn bộ tests: URL normalization, fingerprint stability, cache TTL & eviction, Brave HTTP 200/401/429 mocks, budget gate hard cap, usage metrics accounting, month rollover, shared cache between watches, watch limits 5/30, cadence validation, prompt injection defense, notifier embed formatting, allowed_mentions, slash command safety lock).
  - Còn 7 tests liên quan đến pipeline mô phỏng chạy qua lại giữa mock harness và scheduler cần căn chỉnh assertion:
    1. `test_delete_cascade`: Đổi lệnh gọi kiểm tra `runs` sang `get_recent_runs`.
    2. `test_first_run_silent_baseline`: Kiểm tra số lượng kết quả lưu trong DB.
    3. `test_delta_second_run_no_change_is_quiet`: Khẳng định trạng thái run là `RunStatus.NO_CHANGE.value` (`no_change`) thay vì `success`.
    4. `test_delta_meaningful_change_notifies`: Đồng bộ `last_checked_at` giữa 2 lượt gọi mock để kích hoạt bước 2.
    5. `test_discovered_not_evaluated_recovery`: Kiểm tra danh sách ứng viên pending sau khi AI timeout.
    6. `test_duplicate_event_is_suppressed`: Khẳng định lượt 2 có bài mới nhưng không có biến.
    7. `test_terminal_condition_completes_watch`: Khẳng định trạng thái `completed` sau khi condition được đáp ứng.

---

## 5. HƯỚNG DẪN KHI BẬT LẠI DỰ ÁN ĐỂ TIẾP TỤC

Khi bạn sẵn sàng tiếp tục công việc, bạn chỉ cần thực hiện:

```bash
# 1. Chuyển sang branch tạm
git checkout feat/asumi-watch-v2.9-wip

# 2. Chạy kiểm tra bộ test
python -m unittest tests/test_watch.py
```

Sau đó nhắn cho AI:
> *"Tiếp tục hoàn thiện 7 test assertions còn lại trong `tests/test_watch.py` và chuẩn bị báo cáo hoàn tất v2.9.0"*.

Toàn bộ ngữ cảnh sẽ được tiếp nối trơn tru và chính xác!
